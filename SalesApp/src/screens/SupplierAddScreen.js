import { StyleSheet, Text, View } from 'react-native';
import React from 'react';

const SupplierAddScreen = () => {
  return (
    <View>
      <Text>SupplierAddScreen</Text>
    </View>
  );
};

export default SupplierAddScreen;

const styles = StyleSheet.create({});
